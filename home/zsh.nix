{ ... }:
{
  programs.zsh = {
    enable = true;

    autosuggestion = {
      enable = true;
    };

    historySubstringSearch = {
      enable = true;
    };

    syntaxHighlighting = {
      enable = true;
    };
  };
}
